name: CI

on:
  push:
    branches: [main]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  lint:
    name: fmt + clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - name: Install Linux tray build deps
        run: sudo apt-get install -y libgtk-3-dev libayatana-appindicator3-dev libxdo-dev
      - run: cargo fmt --all -- --check
      - run: cargo clippy --workspace --all-targets -- -D warnings

  test:
    name: test (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Install Linux tray build + runtime deps (incl. xvfb for GUI smoke)
        if: matrix.os == 'ubuntu-latest'
        run: sudo apt-get install -y libgtk-3-dev libayatana-appindicator3-dev libxdo-dev xvfb
      - run: cargo test --workspace
      - name: GUI smoke (tray init) — Linux under xvfb
        if: matrix.os == 'ubuntu-latest'
        run: xvfb-run -a cargo test --workspace -- --ignored connect_tray
      - name: GUI smoke (tray init) — Windows
        if: matrix.os == 'windows-latest'
        run: cargo test --workspace -- --ignored connect_tray
